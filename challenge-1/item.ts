export class Item {
    name: string;
    size: string;
    quantity: number;
    price: number;

    constructor(name: string, size: string, quantity: number, price: number) {
        this.name = name
        this.size = size
        this.quantity = quantity
        this.price = price
    }
}
